# 灵汐大陆品牌与世界观 MVP 工作日志

## 本批目标

把技术合并版从“养成好习惯”功能应用的外观，推进为《灵汐大陆》的可识别游戏体验：统一品牌、首次启动讲清世界观、让灵伴选择和日常反馈使用同一套心光叙事。

## 已完成

- 主标题统一为“灵汐大陆”，副文案为“让每个好习惯，点亮一颗星”。
- 更新网页 title、PWA name/short_name、登录页品牌和玩法说明入口。
- 新增首次启动序章：浊气侵蚀大陆 → 好习惯产生心光 → 愿望球召唤灵伴 → 从第一步开始冒险。
- 序章可跳过、只自动展示一次，并可从“灵汐大陆玩法”入口重播。
- 为 8 只普通灵伴补充地域、性格与职责标签，并显示在选宠页和宠物首页。
- 7 只非焰狼普通灵伴已补齐独立逐帧资源：idle、happy、eat、tired、levelUp；焰狼继续使用已验证的精灵动画。
- 播放器已从单一焰狼契约扩展为按物种加载 `/anim/<species>/contract.json`，每只普通灵伴拥有独立资源目录和事件契约。

## 验证

- `node --check public/app.js`：通过。
- `node --check public/anim/player.js`：通过。
- `public/manifest.json` JSON 解析：通过。
- `npm test`：218 通过 / 0 失败。
- `npm run test:anim`：68 通过 / 0 失败。

## 动画批次追加（2026-09-08）

- 7 只非焰狼普通灵伴共 140 帧独立逐帧资源（4+4+4+4+6 帧/物种），均经过 magenta 清理、帧提取、锚点对齐和严格 QC。
- 运行时只保留 `frame-*.png` 与 `contract.json`；raw sheet、透明 sheet、GIF、pipeline meta 归档到 `docs/animation-assets-archive/<species>/<action>/`。
- Service Worker 缓存版本升至 `hp-v14`，普通灵伴动画资源纳入离线清单。
- 动画测试扩展为多物种契约、帧尺寸和资源可达性检查。

## 星图冒险章节追加（2026-09-08）

- 原三处星图节点扩展为首章 7 日路线：星海关口、赤曜荒原、月见潮汐、影桥回廊、翠雨林地、磐石隘口、星心圣所。
- 孩子端首页改为带连线的 D1-D7 路线图，显示当前日、完成日、锁定节点和毕业后的回访状态。
- 8 只普通灵伴各有一个签名事件；另有约 35% 的确定性随机奇遇强化灵汐大陆叙事，但均不改数值。
- 野外 Boss 难度从低等级友好起步，随等级缓慢增加，避免新手首战必败。
- Service Worker 缓存版本升至 `hp-v16`，完整验收记录见 `docs/adventure-mvp-log.md`。

## 未包含

- 未执行云端部署。
- 未重打 APK；WebView 资源副本需在试玩确认后同步。
- 隐藏神兽仍保留静态 fallback，后续再做独立资源批次。

## 后续试玩重点

1. 清空孩子端 `hp_prologue_seen` 后验证首次启动序章和跳过流程。
2. 重新打开“📖 玩法”，验证序章重播。
3. 分别检查焰狼、雷隼、玄龟、影豹、灵狐、岩熊、棘龙、霜鹰的地域/性格文案和独立升级/投喂/回归反馈。
4. 360px 视口检查序章图片、按钮和选宠卡片无溢出。
