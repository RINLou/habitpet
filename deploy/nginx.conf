# Nginx 反向代理配置
# 用法：把域名替换为你的真实域名，证书路径改好，然后 ln -s 到 /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name habitpet.yourdomain.com;  # 改成你的域名

    # 自动跳 HTTPS（证书配好后启用）
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 预留（以后真推送用）
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 上传照片可能较大
        client_max_body_size 20M;
        proxy_read_timeout 300s;
    }
}

# HTTPS 配置（用 certbot 或腾讯云免费证书）
# server {
#     listen 443 ssl http2;
#     server_name habitpet.yourdomain.com;
#
#     ssl_certificate /etc/letsencrypt/live/habitpet.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/habitpet.yourdomain.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         client_max_body_size 20M;
#         proxy_read_timeout 300s;
#     }
# }
