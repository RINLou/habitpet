# HabitPet 环境变量示例
# 复制为 .env 并修改，生产环境建议用 systemd/PM2 注入而非 .env

NODE_ENV=production
PORT=3000

# 数据目录（默认项目根目录下的 data/）
DATA_DIR=./data

# 会话有效期（天）
SESSION_TTL_DAYS=30

# 可选： stronger random secret for session token entropy
# 留空则使用内置随机生成
# SECRET=
